doctrine:
    dbal:
        driver: pdo_sqlite
        path:   %kernel.root_dir%/cache/database.%kernel.environment%.sqlite
    orm:
        auto_generate_proxy_classes: true
        auto_mapping: true